package com.bejourney.javaoop.accesstest;

public interface Participants {
    //    int num = 0;  // public static final (default), so don't create in interface
    void showParticipants(int participants);
}
